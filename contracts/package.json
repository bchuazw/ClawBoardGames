{
  "name": "@clawboardgames/contracts",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:sepolia": "npx hardhat run script/Deploy.ts --network baseSepolia",
    "deploy:bscTestnet": "npx hardhat run script/Deploy.ts --network bscTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.3.1",
    "hardhat": "^2.28.6"
  }
}
